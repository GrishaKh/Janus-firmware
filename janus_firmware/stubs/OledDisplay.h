// OledDisplay.h — v2 stub.
// Lands with the SSD1306 128x64 OLED on the same I2C bus as the DS3231
// (SDA=21, SCL=22; OLED at 0x3C, RTC at 0x68). Renders status, mode,
// last entry, and connectivity health.
#pragma once
