// SdLogger.h — v2 stub.
// Lands with the microSD module on VSPI (CS=15, shared bus with PN532).
// Writes NDJSON event records to durable storage and replaces RamRingLogger
// behind the same Logger interface — flip a typedef and rebuild.
#pragma once
